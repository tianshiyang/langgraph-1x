[project]
name = "langgraph-1x"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3",
    "langchain-anthropic>=1.4",
    "langchain-core>=1.4",
    "langgraph>=1.2",
    "langgraph-cli[inmem]>=0.4",
    "socksio>=1.0",  # httpx 走 SOCKS 代理(VPN)所需，否则调用模型报 ImportError
    "typing-extensions>=4.15",
]
